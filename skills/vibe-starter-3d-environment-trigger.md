---
name: vibe-starter-3d-environment-trigger
description: |
  Core document on creating interactive trigger areas and timed events using `vibe-starter-3d-environment` in a 3D project.
  
  This document covers:
  - Triggering events when a player enters or exits an area (e.g., opening doors).
  - Continuous area effects (e.g., lava zones dealing damage over time).
  - Periodic events (e.g., spawning monsters at intervals).
  - One-time triggers (e.g., checkpoints).
  - Conditional triggers (e.g., requiring a key to open a door).
  - Line-of-sight detection (e.g., security cameras).
  
  Reference this document when a user wants to implement spatial zone interactions or timed event mechanics.
  
  [Important] You must install the package using `bun add vibe-starter-3d-environment` before you can use it.
---

## Instructions

1. Ensure the package is installed. If not, run: `bun add vibe-starter-3d-environment`
2. Read the documentation file at `node_modules/vibe-starter-3d-environment/docs/read_environment_trigger.md` and follow its instructions.
